export default function Page() {
  return (
    <div className="mx-auto max-w-4xl px-4 py-10">
      <h1 className="text-2xl font-semibold capitalize">overview</h1>
      <p className="text-muted mt-4 leading-7">
        GitNut provides a workflow to import repositories, compute canonical manifests, build reproducible artifacts, store outputs, and anchor proofs on Solana.
      </p>
    </div>
  );
}
