# @gitnut/config

Shared configuration package for the GitNut monorepo.

This package contains:
- ESLint shareable configuration
- TypeScript base configs
- Prettier configuration

> This package is intended to be consumed by other workspace packages via `extends`.
