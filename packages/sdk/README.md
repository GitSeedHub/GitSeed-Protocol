# @gitnut/sdk

TypeScript SDK for interacting with GitNut services and the on-chain GitNut Registry program.

Features:
- Wallet and connection helpers
- PDA derivation helpers
- Instruction builders compatible with Anchor IDL
- Parsers for on-chain accounts and events
- Typed API client for GitNut backend

This package intentionally does not include UI code.
