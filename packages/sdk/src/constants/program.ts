import { PublicKey } from '@solana/web3.js';

/**
 * Default program id placeholder.
 * You should set this at runtime via configuration or by importing the generated IDL wrapper.
 */
export const DEFAULT_GITNUT_REGISTRY_PROGRAM_ID = new PublicKey(
  'G1tNuTReg1stry11111111111111111111111111111111',
);
