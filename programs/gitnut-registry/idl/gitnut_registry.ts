/**
 * Placeholder TypeScript IDL export.
 * Replace by running:
 *   anchor idl fetch -o ./programs/gitnut-registry/idl/gitnut_registry.json <PROGRAM_ID>
 * and then generating types with your preferred pipeline.
 */
export type GitnutRegistryIdl = any;
